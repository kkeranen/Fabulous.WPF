<UserControl x:Class="Sudoku.CS.Regions.RegionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:regions="clr-namespace:Sudoku.CS.Regions"
             xmlns:tiles="clr-namespace:Sudoku.CS.Tiles"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance regions:RegionViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="200" d:DesignWidth="200"
             SnapsToDevicePixels="True">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type tiles:TileViewModel}">
            <tiles:TileView />
        </DataTemplate>
    </UserControl.Resources>
    <Border BorderThickness="1" BorderBrush="Black">
        <ItemsControl ItemsSource="{Binding Tiles}" >
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid  />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </Border>
</UserControl>
